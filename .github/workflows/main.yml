name: Windows-RDP-Secure-Telegram-TCP

on:
  workflow_dispatch:
    inputs:
      duration_minutes:
        description: 'مدة التشغيل بالدقائق (360 max)'
        required: false
        default: '360'

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: Download Cloudflared
        run: curl -L --output cloudflared.exe https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe

      - name: Check Secrets
        shell: pwsh
        run: |
          if ("${{ secrets.RDP_USERNAME }}" -eq "") { Write-Error "Missing RDP_USERNAME"; exit 1 }
          if ("${{ secrets.RDP_PASSWORD }}" -eq "") { Write-Error "Missing RDP_PASSWORD"; exit 1 }
          if ("${{ secrets.TELEGRAM_BOT_TOKEN }}" -eq "") { Write-Error "Missing TELEGRAM_BOT_TOKEN"; exit 1 }
          if ("${{ secrets.TELEGRAM_CHAT_ID }}" -eq "") { Write-Error "Missing TELEGRAM_CHAT_ID"; exit 1 }

      - name: Enable RDP and Create User
        shell: pwsh
        run: |
          $user = "${{ secrets.RDP_USERNAME }}"
          $pass = "${{ secrets.RDP_PASSWORD }}"
          Write-Host "::add-mask::$user"
          Write-Host "::add-mask::$pass"
          net user $user $pass /add
          net localgroup administrators $user /add
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Restart-Service TermService -Force
          Start-Sleep -Seconds 5
          if (-not (Get-NetTCPConnection -LocalPort 3389 -ErrorAction SilentlyContinue)) {
            Write-Error "Port 3389 not listening"
            exit 1
          }
          Write-Host "RDP is ready"

      - name: Start Tunnel and Capture URL (Fixed)
        shell: pwsh
        run: |
          $stderrLog = "cloudflared_stderr.log"
          $stdoutLog = "cloudflared_stdout.log"
          $urlFile = "tunnel_url.txt"
          Remove-Item -Force $stderrLog, $stdoutLog, $urlFile -ErrorAction SilentlyContinue

          # تشغيل العملية مع توجيه stderr إلى ملف و stdout إلى ملف آخر
          $proc = Start-Process -FilePath ".\cloudflared.exe" `
            -ArgumentList "tunnel --protocol auto --url tcp://localhost:3389" `
            -WindowStyle Hidden -PassThru `
            -RedirectStandardOutput $stdoutLog -RedirectStandardError $stderrLog

          # البحث عن الرابط في ملف stderr (يظهر هناك غالباً)
          $found = $false
          $timeout = 120
          for ($i = 0; $i -lt $timeout; $i++) {
            Start-Sleep -Seconds 1
            if (Test-Path $stderrLog) {
              $content = Get-Content $stderrLog -Raw -ErrorAction SilentlyContinue
              if ($content -match '(https://[a-z0-9-]+\.trycloudflare\.com)') {
                $url = $matches[1]
                $found = $true
                break
              }
            }
          }

          if (-not $found) {
            Write-Host "ERROR: Could not find tunnel URL."
            Write-Host "--- STDERR content ---"
            Get-Content $stderrLog -ErrorAction SilentlyContinue
            Write-Host "--- STDOUT content ---"
            Get-Content $stdoutLog -ErrorAction SilentlyContinue
            exit 1
          }

          # إخفاء الرابط وحفظه
          Write-Host "::add-mask::$url"
          $url | Out-File -FilePath $urlFile -Encoding ascii
          $proc.Id | Out-File -FilePath tunnel.pid
          Write-Host "Tunnel ready, URL captured and hidden."

      - name: Send Connection Instructions to Telegram
        shell: pwsh
        run: |
          $url = Get-Content tunnel_url.txt -Raw
          $hostname = $url -replace 'https://',''
          $user = "${{ secrets.RDP_USERNAME }}"
          $pass = "${{ secrets.RDP_PASSWORD }}"
          $duration = "${{ github.event.inputs.duration_minutes }}"
          $msg = "✅ RDP Tunnel Ready%0A%0A"
          $msg += "🔗 Tunnel URL: $url%0A%0A"
          $msg += "📥 Instructions:%0A"
          $msg += "1. Download cloudflared from: https://github.com/cloudflare/cloudflared/releases%0A"
          $msg += "2. Open terminal in that folder%0A"
          $msg += "3. Run: cloudflared access tcp --hostname $hostname --url localhost:3333%0A"
          $msg += "4. Keep terminal open%0A"
          $msg += "5. Open Remote Desktop (mstsc) -> localhost:3333%0A"
          $msg += "6. Username: $user%0A"
          $msg += "   Password: $pass%0A"
          $msg += "7. Accept certificate warning%0A%0A"
          $msg += "⏱️ Tunnel active for $duration minutes."
          $api = "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage"
          $body = "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=$msg"
          Invoke-RestMethod -Uri $api -Method Post -Body $body -ContentType "application/x-www-form-urlencoded" | Out-Null
          Write-Host "Instructions sent to Telegram"

      - name: Keep Runner Alive
        shell: pwsh
        run: |
          $minutes = [int]${{ github.event.inputs.duration_minutes }}
          if ($minutes -gt 360) { $minutes = 360 }
          Write-Host "Runner active for $minutes minutes"
          Start-Sleep -Seconds ($minutes * 60)

      - name: Cleanup
        shell: pwsh
        if: always()
        run: |
          if (Test-Path tunnel.pid) {
            $pid = Get-Content tunnel.pid
            Stop-Process -Id $pid -Force -ErrorAction SilentlyContinue
          }
          Get-Process -Name cloudflared -ErrorAction SilentlyContinue | Stop-Process -Force
          $user = "${{ secrets.RDP_USERNAME }}"
          if ($user) { net user $user /delete 2>$null }
          Write-Host "Cleanup finished"
