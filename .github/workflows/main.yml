name: Windows-RDP-Secure-Telegram

on:
  workflow_dispatch:
    inputs:
      duration_minutes:
        description: 'مدة التشغيل بالدقائق'
        required: false
        default: '360'

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: Download Cloudflared
        run: curl -L --output cloudflared.exe https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe

      - name: Check Secrets
        shell: pwsh
        run: |
          if ("${{ secrets.RDP_USERNAME }}" -eq "") { Write-Error "Missing RDP_USERNAME"; exit 1 }
          if ("${{ secrets.RDP_PASSWORD }}" -eq "") { Write-Error "Missing RDP_PASSWORD"; exit 1 }
          if ("${{ secrets.TELEGRAM_BOT_TOKEN }}" -eq "") { Write-Error "Missing TELEGRAM_BOT_TOKEN"; exit 1 }
          if ("${{ secrets.TELEGRAM_CHAT_ID }}" -eq "") { Write-Error "Missing TELEGRAM_CHAT_ID"; exit 1 }
          Write-Host "All secrets are present"

      - name: Setup RDP and User
        shell: pwsh
        run: |
          $user = "${{ secrets.RDP_USERNAME }}"
          $pass = "${{ secrets.RDP_PASSWORD }}"
          Write-Host "::add-mask::$user"
          Write-Host "::add-mask::$pass"
          net user $user $pass /add
          net localgroup administrators $user /add
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Start-Service TermService

      - name: Start Tunnel and Get URL
        shell: pwsh
        run: |
          # تشغيل النفق مع توجيه كلا المخرجين
          $proc = Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --url rdp://localhost:3389" -NoNewWindow -PassThru -RedirectStandardError stderr.txt -RedirectStandardOutput stdout.txt
          
          # انتظار الرابط (الحد الأقصى 90 ثانية)
          $url = $null
          for ($i = 0; $i -lt 45; $i++) {
            Start-Sleep -Seconds 2
            # ابحث في stderr ثم stdout
            $content = Get-Content stderr.txt -ErrorAction SilentlyContinue
            if (-not $content) { $content = Get-Content stdout.txt -ErrorAction SilentlyContinue }
            if ($content -match 'https://[a-z0-9-]+\.trycloudflare\.com') {
              $url = $matches[0]
              Write-Host "Found URL: $url"
              break
            }
          }
          
          if (-not $url) {
            Write-Host "ERROR: Could not find tunnel URL within timeout."
            Write-Host "--- STDERR ---"
            Get-Content stderr.txt -ErrorAction SilentlyContinue
            Write-Host "--- STDOUT ---"
            Get-Content stdout.txt -ErrorAction SilentlyContinue
            exit 1
          }
          
          $url | Out-File -FilePath tunnel_url.txt -Encoding ascii
          # حفظ PID لإنهاء العملية لاحقاً
          $proc.Id | Out-File -FilePath tunnel.pid

      - name: Send to Telegram
        shell: pwsh
        run: |
          $url = Get-Content tunnel_url.txt -Raw
          $user = "${{ secrets.RDP_USERNAME }}"
          $pass = "${{ secrets.RDP_PASSWORD }}"
          $duration = "${{ github.event.inputs.duration_minutes }}"
          $msg = "RDP Ready%0AURL: $url%0AUser: $user%0APass: $pass%0ADuration: $duration min"
          $api = "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage"
          $body = "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=$msg"
          Invoke-RestMethod -Uri $api -Method Post -Body $body -ContentType "application/x-www-form-urlencoded" | Out-Null
          Write-Host "Message sent to Telegram"

      - name: Keep Alive
        shell: pwsh
        run: |
          $minutes = [int]${{ github.event.inputs.duration_minutes }}
          if ($minutes -gt 360) { $minutes = 360 }
          Write-Host "Keeping alive for $minutes minutes"
          Start-Sleep -Seconds ($minutes * 60)

      - name: Cleanup
        shell: pwsh
        if: always()
        run: |
          if (Test-Path tunnel.pid) {
            $pid = Get-Content tunnel.pid
            Stop-Process -Id $pid -Force -ErrorAction SilentlyContinue
          }
          $user = "${{ secrets.RDP_USERNAME }}"
          if ($user) { net user $user /delete 2>$null }
          Write-Host "Cleanup finished"
